# Load the devenv environment through direnv.
# Uses the direnvrc bundled with the locally-installed devenv, so it
# always matches this machine's devenv version (no pinned hash to bump).
# https://devenv.sh/integrations/direnv/
eval "$(devenv direnvrc)"

use devenv
